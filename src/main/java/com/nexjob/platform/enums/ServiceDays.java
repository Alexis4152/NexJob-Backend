package com.nexjob.platform.enums;

/** Dias en los que el prestador ofrece sus servicios (informativo, capturado por el mismo). */
public enum ServiceDays {
    LUN_VIE,
    LUN_SAB,
    LUN_DOM,
    SAB_DOM
}
